{
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3"
  },

  "name": "fortis-budget",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "node update-version.js && vite build",
    "preview": "vite preview"
  }
}
